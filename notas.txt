

git init

git status

gid add . 

git commit -m "Primer commit"

heroku login

heroku git:remote -a koke-webpage

git push heroku master

heroku open

git add . 

git status 

git commit -m "Cambios 2020-0828 06:00 pm"

git remote add origin https://github.com/KokeSky/webserver-express.git

git remote

git push -u origin master

git push heroku master

git tag -a v1.0.0 -m "Primera version del proyecto"

git push --tags

****

heroku create -h 
heroku create     nos crea el nombre heroku
git remote -v
git push heroku master
heroku open

----
git add .

git commit  -m "RestServer - Usuario"

git push heroku master

heroku open

---

git status

git push

git push origin master

git tags

git tag -a v0.0.3 -m "Usuario y RESTServer"

git push --tags


---
heroku config

heroku config:set nombre="Jorge Luis"

heroku config:unset nombre


Marlin para tacos gobernador
Ingredientes 
750 grms de marlin o atun ahumado
250 grms de tocino o panceta
500 grms de tomate
4 chiles verdes tatemar y picar en cuadros
2 cebollas 
pimienta al gusto
( tip  no se le pone sal )
















































